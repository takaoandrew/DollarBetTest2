<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andrewtakao.dollarbettest2">

    <application
        android:allowBackup="true"
        android:icon="@drawable/db3"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FriendDetailActivity"
            android:label="@string/edit_fragment_title"
            android:parentActivityName=".FriendsActivity" />
        <activity
            android:name=".AppPreferences"
            android:label="@string/title_preferences"
            android:parentActivityName=".FriendsActivity" />
        <activity android:name=".NewbieActivity" />
        <activity android:name=".FriendsActivity" />
        <activity android:name=".BetsActivity" />
        <activity
            android:name=".BetDetailActivity"
            android:label="Bets"
            android:parentActivityName=".BetsActivity" />
    </application>

</manifest>